function capitalize(result) {
    var maj = result.map((x) => x.toUpperCase())
    return maj
    }
    const result = ["CHANGE", "this", "array", "BY", "ThE", "RESULT", "oF", "youR", "FUNCTION", "TO", "test", "IT", "OUT"]
    displayResult(capitalize(result))
    //Mopek
    